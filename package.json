{
  "name": "stackit",
  "version": "0.1.0",
  "description": "Minimal, fast, and deterministic — a Git-based dependency manager.",
  "license": "MIT",
  "author": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/user/stackit.git"
  },
  "keywords": [
    "git",
    "dependencies",
    "vendor",
    "monorepo",
    "cli"
  ],
  "type": "module",
  "bin": {
    "stackit": "./dist/bin/stackit.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup",
    "prepack": "npm run build",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier -w ."
  },
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}
